on_http_request:
  # Require Google login
  - actions:
      - type: oauth
        config:
          provider: google

  # Deny anyone not in the allowlist
  - expressions:
      - >
        !(actions.ngrok.oauth.identity.email in [
          "somemail@gmail.com"
        ])
    actions:
      - type: deny
