on_http_request:
  # Require Google login
  - actions:
      - type: oauth
        config:
          provider: google

  # Deny anyone not in the allowlist
  - expressions:
      - >
        !(actions.ngrok.oauth.identity.email in [
          "rahulmutt@gmail.com",
          "rahulmutti64@gmail.com",
          "jyothsnasrinivas17@gmail.com"
        ])
    actions:
      - type: deny
