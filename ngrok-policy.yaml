on_http_request:
  # Step 1: Require Google login
  - actions:
      - type: oauth
        config:
          provider: google

  # Step 2: Allow only specific emails
  - expressions:
      - |
        actions.ngrok.oauth.identity.email in [
          "rahulmutt@gmail.com",
          "rahulmutti64@gmail.com",
          "jyothsnasrinivas17@gmail.com"
        ]
    actions:
      - type: forward

  # Step 3: Deny everyone else
  - actions:
      - type: deny
